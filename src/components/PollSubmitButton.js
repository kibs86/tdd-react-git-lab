import React from 'react';

class PollSubmitButton extends React.Component{
    render(){
        return (
            <button className="btn btn-primary" type="submit" value="Submit"> Go </button>
        );
    }
}

export default PollSubmitButton;